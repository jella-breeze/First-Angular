import { NgBootstrapAlertInterface } from './ng-bootstrap-alert';
import { NgBootstrapAlertService } from './ng-bootstrap-alert.service';
export declare class NgBootstrapAlertComponent {
    private bootstrapAlertService;
    alertPosition: string;
    bootstrapAlerts: Array<NgBootstrapAlertInterface>;
    constructor(bootstrapAlertService: NgBootstrapAlertService);
    readonly DEFAULT_TIMEOUT: number;
    readonly FADE_TIMEOUT: number;
    show(bootstrapAlert: NgBootstrapAlertInterface): void;
    fade(bootstrapAlert: any): void;
    destroy(uuid: string): void;
    findIndex(array: any, attr: any, value: any): number;
}
